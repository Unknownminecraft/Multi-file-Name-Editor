🌟Multi-file Name Changer, this will allow user to edit the file name and text inside the file. This editor tool is made for `.json` file, may not work with other file format but

# version 5.1
• added Edit both text inside & file.
• line 180 "...accept=".json" sty..." changed to  "...accept=".lang" sty..."  and it accepts all types of files, lol.


# version 5.2
• can edit end text→ 1. upload files > in "replace file name" > enter text you want to replace in "old text" only at top > then click "rename
